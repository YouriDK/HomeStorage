org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Single source of truth for the app version. versionCode is derived from it in
# app/build.gradle.kts: Play requires it to grow strictly and never repeat, so
# every upload needs a PATCH bump here.
boxpix.version=1.0.4
